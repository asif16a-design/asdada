local Players=game:GetService("Players");local TweenService=game:GetService("TweenService");local RunService=game:GetService("RunService");local UserInputService=game:GetService("UserInputService");local Debris=game:GetService("Debris");local player=Players.LocalPlayer;if player.Name~="LIGHTWARRIOR_22" then return end;local camera=workspace.CurrentCamera;local character,humanoid,root;local function setupCharacter(c)character=c;humanoid=c:WaitForChild("Humanoid");root=c:WaitForChild("HumanoidRootPart")end;setupCharacter(player.Character or player.CharacterAdded:Wait());player.CharacterAdded:Connect(setupCharacter);local POWER=50;local MAX_POWER=200;local MIN_POWER=10;local COOLDOWN=4;local busy=false;local gui=Instance.new("ScreenGui");gui.Name="HollowPurpleSystem";gui.ResetOnSpawn=false;gui.IgnoreGuiInset=true;gui.Parent=player:WaitForChild("PlayerGui");local main=Instance.new("Frame");main.Size=UDim2.fromOffset(270,355);main.Position=UDim2.new(1,-290,0,35);main.BackgroundColor3=Color3.fromRGB(12,8,20);main.BackgroundTransparency=.08;main.BorderSizePixel=0;main.Parent=gui;local corner=Instance.new("UICorner",main);corner.CornerRadius=UDim.new(0,18);local stroke=Instance.new("UIStroke",main);stroke.Color=Color3.fromRGB(180,90,255);stroke.Thickness=1.5;stroke.Transparency=.2;local title=Instance.new("TextLabel",main);title.Size=UDim2.new(1,-20,0,42);title.Position=UDim2.fromOffset(10,5);title.BackgroundTransparency=1;title.Text="✦  H O L L O W   P U R P L E";title.TextColor3=Color3.fromRGB(235,205,255);title.TextSize=15;title.Font=Enum.Font.GothamBold;local powerLabel=Instance.new("TextLabel",main);powerLabel.Size=UDim2.new(1,-100,0,30);powerLabel.Position=UDim2.fromOffset(15,48);powerLabel.BackgroundTransparency=1;powerLabel.Text="POWER  •  50";powerLabel.TextColor3=Color3.fromRGB(190,160,220);powerLabel.TextSize=13;powerLabel.Font=Enum.Font.GothamMedium;powerLabel.TextXAlignment=Enum.TextXAlignment.Left;local minus=Instance.new("TextButton",main);minus.Size=UDim2.fromOffset(34,30);minus.Position=UDim2.new(1,-88,0,47);minus.Text="−";minus.TextSize=20;minus.Font=Enum.Font.GothamBold;minus.TextColor3=Color3.new(1,1,1);minus.BackgroundColor3=Color3.fromRGB(45,25,65);Instance.new("UICorner",minus).CornerRadius=UDim.new(0,8);local plus=minus:Clone();plus.Name="Plus";plus.Position=UDim2.new(1,-48,0,47);plus.Text="+";plus.Parent=main;local function updatePower()powerLabel.Text="POWER  •  "..POWER end;minus.MouseButton1Click:Connect(function()POWER=math.max(MIN_POWER,POWER-10);updatePower()end);plus.MouseButton1Click:Connect(function()POWER=math.min(MAX_POWER,POWER+10);updatePower()end);local dragging=false;local dragStart,startPos;local function beginDrag(input)dragging=true;dragStart=input.Position;startPos=main.Position end;local function updateDrag(input)if not dragging then return end;local delta=input.Position-dragStart;main.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset+delta.X,startPos.Y.Scale,startPos.Y.Offset+delta.Y)end;main.InputBegan:Connect(function(input)if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then beginDrag(input)end end);UserInputService.InputChanged:Connect(function(input)if input.UserInputType==Enum.UserInputType.MouseMovement or input.UserInputType==Enum.UserInputType.Touch then updateDrag(input)end end);UserInputService.InputEnded:Connect(function(input)if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then dragging=false end end);local function sphere(position,size,color,life,material)local p=Instance.new("Part");p.Shape=Enum.PartType.Ball;p.Anchored=true;p.CanCollide=false;p.CanTouch=false;p.CanQuery=false;p.Material=material or Enum.Material.Neon;p.Color=color;p.Size=Vector3.new(size,size,size);p.CFrame=CFrame.new(position);p.Parent=workspace;Debris:AddItem(p,life);return p end;local function beamPart(a,b,width,color,life)local p=Instance.new("Part");p.Anchored=true;p.CanCollide=false;p.CanTouch=false;p.CanQuery=false;p.Material=Enum.Material.Neon;p.Color=color;p.Size=Vector3.new(width,width,(a-b).Magnitude);p.CFrame=CFrame.lookAt((a+b)/2,b);p.Parent=workspace;Debris:AddItem(p,life);return p end;local function ring(position,size,color,life)local p=Instance.new("Part");p.Shape=Enum.PartType.Cylinder;p.Anchored=true;p.CanCollide=false;p.CanTouch=false;p.CanQuery=false;p.Material=Enum.Material.Neon;p.Color=color;p.Transparency=.15;p.Size=Vector3.new(.15,size,size);p.CFrame=CFrame.new(position)*CFrame.Angles(0,0,math.rad(90));p.Parent=workspace;TweenService:Create(p,TweenInfo.new(life,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),{Size=Vector3.new(.15,size*3,size*3),Transparency=1}):Play();Debris:AddItem(p,life+.1)end;local function shockwave(position,power)for i=1,5 do task.delay(i*.05,function()ring(position+Vector3.new(0,i*.25,0),power*(i*.5),Color3.fromRGB(150,50,255),.45)end)end end;local function cameraShake(amount,time)local start=tick();local connection;connection=RunService.RenderStepped:Connect(function()local elapsed=tick()-start;if elapsed>=time then connection:Disconnect();return end;local fade=1-elapsed/time;camera.CFrame*=CFrame.Angles(math.rad((math.random()-.5)*amount*fade),math.rad((math.random()-.5)*amount*fade),math.rad((math.random()-.5)*amount*fade))end)end;local function sound(id,volume,speed,parent)local s=Instance.new("Sound");s.SoundId="rbxassetid://"..id;s.Volume=volume or 1;s.PlaybackSpeed=speed or 1;s.RollOffMaxDistance=150;s.Parent=parent or root;s:Play();Debris:AddItem(s,8)end;local S1=9120386436;local S2=9118828566;local S3=9047050075;local S4=9047101374;local S5=9113420771;local cooldownText=Instance.new("TextLabel",main);cooldownText.Size=UDim2.new(1,-30,0,25);cooldownText.Position=UDim2.fromOffset(15,320);cooldownText.BackgroundTransparency=1;cooldownText.Text="READY";cooldownText.TextColor3=Color3.fromRGB(170,255,210);cooldownText.TextSize=12;cooldownText.Font=Enum.Font.GothamBold;local function cooldown()for i=COOLDOWN,1,-1 do cooldownText.Text="COOLDOWN  •  "..i.."s";cooldownText.TextColor3=Color3.fromRGB(255,180,100);task.wait(1)end;cooldownText.Text="READY";cooldownText.TextColor3=Color3.fromRGB(170,255,210);busy=false end;local function abilityButton(text,y)local b=Instance.new("TextButton",main);b.Size=UDim2.new(1,-30,0,58);b.Position=UDim2.fromOffset(15,y);b.BackgroundColor3=Color3.fromRGB(31,18,48);b.TextColor3=Color3.fromRGB(240,225,255);b.TextSize=14;b.Font=Enum.Font.GothamBold;b.Text=text;b.AutoButtonColor=false;b.Parent=main;Instance.new("UICorner",b).CornerRadius=UDim.new(0,12);local s=Instance.new("UIStroke",b);s.Color=Color3.fromRGB(115,55,180);s.Transparency=.35;return b end;local purple=Color3.fromRGB(165,70,255);local blue=Color3.fromRGB(65,120,255);local red=Color3.fromRGB(255,65,100);local b1=abilityButton("①  PURPLE ORB",85);local function PurpleOrb()if busy or not root then return end;busy=true;sound(S1,1.2,.9);local start=root.Position+root.CFrame.LookVector*5+Vector3.new(0,2,0);local orb=sphere(start,2,purple,5);local light=Instance.new("PointLight",orb);light.Color=purple;light.Brightness=8;light.Range=25;TweenService:Create(orb,TweenInfo.new(.55,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{Size=Vector3.new(POWER/8,POWER/8,POWER/8)}):Play();for i=1,14 do task.delay(i*.035,function()local angle=i*math.pi/7;sphere(start+Vector3.new(math.cos(angle)*3,math.sin(angle)*3,0),.45,blue,.35)end)end;shockwave(start,POWER/2);cameraShake(2.5,.25);local target=start+root.CFrame.LookVector*(POWER*3);TweenService:Create(orb,TweenInfo.new(.75,Enum.EasingStyle.Quart,Enum.EasingDirection.In),{Position=target,Size=Vector3.new(2,2,2)}):Play();task.wait(.75);sound(S2,1.5,.75);shockwave(target,POWER);for i=1,20 do sphere(target+Vector3.new(math.random(-8,8),math.random(-5,8),math.random(-8,8)),math.random(1,3),purple,.5)end;cameraShake(6,.35);orb:Destroy();cooldown()end;b1.MouseButton1Click:Connect(PurpleOrb);local b2=abilityButton("②  HOLLOW CRUSH",153);local function HollowCrush()if busy or not root then return end;busy=true;sound(S3,1.3,.8);local pos=root.Position+root.CFrame.LookVector*12;local core=sphere(pos,3,red,4);for i=1,12 do local angle=i*math.pi*2/12;local p=pos+Vector3.new(math.cos(angle)*9,math.sin(angle)*9,0);local line=beamPart(p,pos,.35,blue,.45);TweenService:Create(line,TweenInfo.new(.4),{Size=Vector3.new(.05,.05,(p-pos).Magnitude)}):Play()end;shockwave(pos,POWER);cameraShake(5,.3);task.wait(.5);sound(S4,1.4,.7);for i=1,8 do ring(pos,POWER*(i/2),purple,.5)end;TweenService:Create(core,TweenInfo.new(.4,Enum.EasingStyle.Exponential,Enum.EasingDirection.In),{Size=Vector3.new(.2,.2,.2),Transparency=1}):Play();cameraShake(9,.45);for i=1,25 do sphere(pos+Vector3.new(math.random(-POWER/2,POWER/2),math.random(-POWER/3,POWER/2),math.random(-POWER/2,POWER/2)),math.random(.3,1.5),Color3.fromRGB(math.random(90,190),math.random(30,100),255),.7)end;task.wait(.5);if core then core:Destroy()end;cooldown()end;b2.MouseButton1Click:Connect(HollowCrush);local b3=abilityButton("③  ✦ HOLLOW PURPLE ✦",221);local function HollowPurple()if busy or not root then return end;busy=true;local oldWalk=humanoid.WalkSpeed;humanoid.WalkSpeed=4;sound(S5,1.5,.65);local center=root.Position+root.CFrame.LookVector*8+Vector3.new(0,3,0);local blueOrb=sphere(center-root.CFrame.RightVector*5,1,blue,6);local redOrb=sphere(center+root.CFrame.RightVector*5,1,red,6);local bl=Instance.new("PointLight",blueOrb);bl.Color=blue;bl.Brightness=10;bl.Range=30;local rl=Instance.new("PointLight",redOrb);rl.Color=red;rl.Brightness=10;rl.Range=30;TweenService:Create(blueOrb,TweenInfo.new(.8,Enum.EasingStyle.Quart),{Position=center-root.CFrame.RightVector*1.2,Size=Vector3.new(4,4,4)}):Play();TweenService:Create(redOrb,TweenInfo.new(.8,Enum.EasingStyle.Quart),{Position=center+root.CFrame.RightVector*1.2,Size=Vector3.new(4,4,4)}):Play();for i=1,20 do task.delay(i*.035,function()sphere(center+Vector3.new(math.random(-5,5),math.random(-5,5),math.random(-5,5)),.35,i%2==0 and blue or red,.5)end)end;task.wait(.9);sound(S2,1.8,.55);local fusion=sphere(center,3,purple,5);TweenService:Create(blueOrb,TweenInfo.new(.3),{Position=center}):Play();TweenService:Create(redOrb,TweenInfo.new(.3),{Position=center}):Play();task.wait(.35);blueOrb:Destroy();redOrb:Destroy();fusion.Size=Vector3.new(7,7,7);shockwave(center,POWER);cameraShake(8,.4);local direction=root.CFrame.LookVector;local destination=center+direction*(POWER*5);local projectile=sphere(center,POWER/3,purple,4);local light=Instance.new("PointLight",projectile);light.Color=purple;light.Brightness=20;light.Range=50;local start=projectile.Position;local travel=.9;local t=0;while t<travel and projectile.Parent do local dt=RunService.RenderStepped:Wait();t+=dt;local alpha=math.clamp(t/travel,0,1);projectile.Position=start:Lerp(destination,alpha);if math.random()<.45 then sphere(projectile.Position+Vector3.new(math.random(-4,4),math.random(-4,4),math.random(-4,4)),math.random(.2,1),purple,.25)end end;projectile.Position=destination;sound(S1,2,.5);shockwave(destination,POWER*1.5);cameraShake(12,.7);for i=1,40 do local offset=Vector3.new(math.random(-POWER,POWER),math.random(-POWER,POWER),math.random(-POWER,POWER));local fragment=sphere(destination+offset,math.random(.3,2),purple,.8);TweenService:Create(fragment,TweenInfo.new(.7),{Position=destination+offset*2,Transparency=1}):Play()end;for i=1,10 do task.delay(i*.04,function()ring(destination,POWER*(i/2),purple,.65)end)end;TweenService:Create(fusion,TweenInfo.new(.35),{Size=Vector3.new(.1,.1,.1),Transparency=1}):Play();projectile:Destroy();task.wait(.7);humanoid.WalkSpeed=oldWalk;cooldown()end;b3.MouseButton1Click:Connect(HollowPurple);UserInputService.InputBegan:Connect(function(input,gp)if gp then return end;if input.KeyCode==Enum.KeyCode.Z then PurpleOrb()elseif input.KeyCode==Enum.KeyCode.X then HollowCrush()elseif input.KeyCode==Enum.KeyCode.C then HollowPurple()end end);main.Size=UDim2.fromOffset(270,0);TweenService:Create(main,TweenInfo.new(.7,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{Size=UDim2.fromOffset(270,355)}):Play();task.spawn(function()while gui.Parent do stroke.Transparency=.15+.25*(1-(.5+math.sin(os.clock()*2)*.5));task.wait(.05)end end)
